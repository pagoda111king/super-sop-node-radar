# Classification Model Summary

Classify every external project as:

- a core node capability
- a cross-cutting rail capability
- a compound pattern capability
- a design reference

Fit levels:

```text
primary
supporting
adapter
infrastructure
reference
```

Do not create a new node class until the project cannot fit existing nodes, rails, or compound patterns.

